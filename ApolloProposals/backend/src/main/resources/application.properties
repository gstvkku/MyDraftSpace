spring.datasource.url=jdbc:postgresql://localhost:5432/apolloproposals
spring.datasource.username=postgres
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
# You can also try "update" instead of "create-drop" if you want to avoid dropping data each time

# Run only data.sql
#spring.sql.init.mode=always
#spring.sql.init.data-locations=classpath:/data.sql

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework=DEBUG
logging.level.com.seuprojeto=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
openai.api.key= /* AI KEY */


spring.cloud.function.scan.enabled=false
